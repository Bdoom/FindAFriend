<%= form_with(model: conversation, local: true) do |f| %>
  <%= f.select :recipient_id, current_user.friends.collect {|c| [ c.first_name, c.id ] } %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
